version: '3.7'

services:

    app:
        build: .
        image: pwnedhub-dev
        expose:
            - 5000
        volumes:
            - .:/pwnedhub
        environment:
            - CONFIG=Production
            - DATABASE_HOST=db
        command: gunicorn --bind 0.0.0.0:5000 --worker-class eventlet --workers 1 pwnedhub.wsgi:app --error-logfile - --log-level DEBUG
        depends_on:
            - db

    spa:
        build: .
        image: pwnedhub-dev
        expose:
            - 5001
        volumes:
            - .:/pwnedhub
        environment:
            - CONFIG=Production
            - DATABASE_HOST=db
        command: gunicorn --bind 0.0.0.0:5001 pwnedspa.wsgi:app --error-logfile - --log-level DEBUG
        depends_on:
            - db

    api:
        image: pwnedhub-dev
        expose:
            - 5002
        volumes:
            - .:/pwnedhub
            - ~/inbox:/tmp/inbox # local mapping to INBOX_PATH
        environment:
            - CONFIG=Production
            - DATABASE_HOST=db
            - INBOX_PATH=/tmp/inbox # writable location for Gunicorn
        command: gunicorn --bind 0.0.0.0:5002 pwnedapi.wsgi:app --error-logfile - --log-level DEBUG
        depends_on:
            - db

    proxy:
        image: nginx:alpine
        ports:
            - 80:80
        volumes:
            - ./proxy/nginx.conf:/etc/nginx/nginx.conf:ro
            - ./proxy/proxy_params:/etc/nginx/proxy_params:ro
        depends_on:
            - app
            - spa
            - api

    db:
        image: mysql:5.7
        environment:
            - MYSQL_ROOT_PASSWORD=adminpass
            - MYSQL_DATABASE=pwnedhub
            - MYSQL_USER=pwnedhub
            - MYSQL_PASSWORD=dbconnectpass
        volumes:
            - ./pwnedhub.sql:/docker-entrypoint-initdb.d/pwnedhub.sql:ro
